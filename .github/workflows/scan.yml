name: Scan multi-TF RSI (crypto)

on:
  # 5 min past every 4h UTC — gives Binance's 4h candle a moment to settle.
  # Note: GitHub Actions cron has 5–15 min real-world lag.
  schedule:
    - cron: '5 */4 * * *'
  workflow_dispatch:

permissions:
  contents: write

concurrency:
  group: scan
  cancel-in-progress: false

jobs:
  scan:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Unit tests
        run: npm test

      - name: Run full market scan
        run: node scan.mjs

      - name: Commit updated scan data
        run: |
          git config user.name  "rsi-scan-bot"
          git config user.email "rsi-scan-bot@users.noreply.github.com"
          git add data/scan.json data/strong.json
          if git diff --cached --quiet; then
            echo "No changes to commit"
          else
            git commit -m "chore(data): update scan.json + strong.json"
            git push
          fi
